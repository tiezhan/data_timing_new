%% calculate success wait
Waitsucs = calsucrate(TimeWait2s,1); %% input timewait data and calculate success rate by DAY
Waitsucs = calsucrate(TimeWait2s,2); %% input timewait data and calculate success rate by week
Waitsucs = calsucrate(TimeWait2s,3); %% input timewait data and calculate success rate by condition